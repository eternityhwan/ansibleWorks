---
# defaults file for docker-role
# Default Variable
DOWNLOAD_DIR: "/home/{{ ansible_user }}/openmsa/opensearch-dashboard/docker"

# Component Variable
COMPONENT_VERSION: 2.4.1
COMPONENT_PORT: 5601

DATA_PATH: "{{ DOWNLOAD_DIR }}/data"
CONFIG_PATH: "{{ DOWNLOAD_DIR }}/config"