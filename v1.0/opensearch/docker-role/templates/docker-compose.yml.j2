version: '3'
services:
    opensearch:
      container_name: opensearch
      image: opensearchproject/opensearch:{{ COMPONENT_VERSION }}
      restart : always
      ports:
        - {{ COMPONENT_PORT }}:9200
        - {{ MONITORING_PORT }}:9600
      volumes:
        - {{ DATA_PATH }}/:/usr/share/opensearch/data