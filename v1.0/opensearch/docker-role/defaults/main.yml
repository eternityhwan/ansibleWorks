---
# defaults file for docker-role
# Default Variable
DOWNLOAD_DIR: "/home/{{ ansible_user }}/openmsa/opensearch/docker"

# Component Variable
COMPONENT_VERSION: 2.4.1
COMPONENT_PORT: 9200
MONITORING_PORT: 9600

DATA_PATH: "{{ DOWNLOAD_DIR }}/data"
CONFIG_PATH: "{{ DOWNLOAD_DIR }}/config"